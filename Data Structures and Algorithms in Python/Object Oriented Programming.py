# always think of logic of solution before googling code, this is how to avoid cheating
# look at other people's code to obtain logic, not the code itself
x = True
floors = [1,3]
# the above two lines are examples of attributes
def x(y):
    pass
# the above two lines are an example of a method
# think of a cellphone as an object, with attributes color, size, weight
# cellphone's behaviors are ring, text, music
# in other words, objects are nouns, behaviors are words
# class is name under which objects and behaviors exist
# methods are under classes
# object == instance == method
# class x:
#       def __init__(self, y):
#           # initialize attributes
#           self.y = y
#       def blah(z):
#           pass