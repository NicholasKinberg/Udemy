# single source shortest path problem: find path between a given vertex (called source) to all other vertices in graph such that total distance between them (source and destination) is minimum
    # BFS
    # Dijkstra's Algorithm
    # Bellman Ford